# Core inference
vllm>=0.6.0,<0.8.0
transformers==4.49.0   # vLLM 0.7.3 needs <5.0; 4.50+ requires torch>=2.6 for torch.load (CVE-2025-32434), our torch is 2.5.1 — pin to 4.49.0 so Math-Shepherd .bin shards load
torch>=2.4.0
accelerate>=0.34.0

# PRM scoring & embeddings
sentence-transformers>=3.0.0

# Data
datasets>=3.0.0
huggingface-hub>=0.25.0

# Utilities
numpy>=1.26.0
scipy>=1.11.0
pyyaml>=6.0
tqdm>=4.66.0
omegaconf>=2.3.0

# Evaluation & math answer extraction
sympy>=1.13.0
math-verify>=0.5.0  # for MATH500 answer comparison; if unavailable, fallback in tasks/math500.py

# Logging
wandb>=0.18.0

# Plotting
matplotlib>=3.8.0
seaborn>=0.13.0
pandas>=2.2.0

# Dev
pytest>=8.0.0
ruff>=0.6.0
