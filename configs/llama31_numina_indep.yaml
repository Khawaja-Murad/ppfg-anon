# Cross-arch v13 extension (llama × numina).
defaults:
  - default

experiment:
  name: "llama31_numina_indep"

model:
  base_model: "meta-llama/Llama-3.1-8B-Instruct"

baseline:
  method: "independent"

task:
  name: "numina_math"
  n_problems: -1
