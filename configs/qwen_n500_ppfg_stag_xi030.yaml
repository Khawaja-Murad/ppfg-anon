# Day-11.5 Phase 1: ξ_min sweep at 0.30 — Opus W2.1 catch.
# Inherits qwen_n500_ppfg_stag.yaml on every axis EXCEPT min_compat_threshold.
# 0.30 is the threshold that caught ~6% of the prior smoke's injections
# (see configs/ppfg_smoke_tk05_antiredund_v2.yaml's lineage note).

defaults:
  - ppfg

experiment:
  name: "qwen_n500_ppfg_stag_xi030"

model:
  base_model: "Qwen/Qwen2.5-7B-Instruct"

ppfg:
  injection_rule: "stagnation"
  min_compat_threshold: 0.30

task:
  name: "math500"
  n_problems: 500
