# Day-8 P3.1 smoke: LLaMA-3.1-8B-Instruct + Math-Shepherd PRM, n=20, seed 42, indep.
# Verifies PRM-compat (CLAUDE.md §7 Contingency B) before full n=500 launch.

defaults:
  - default

experiment:
  name: "llama31_smoke_math500"
  seed: 42

model:
  base_model: "meta-llama/Llama-3.1-8B-Instruct"
  gpu_memory_utilization: 0.50

baseline:
  method: "independent"

task:
  name: "math500"
  n_problems: 20
