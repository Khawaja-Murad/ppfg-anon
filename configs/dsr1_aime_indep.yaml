# Cross-arch v13 extension (dsr1 × aime).
defaults:
  - default

experiment:
  name: "dsr1_aime_indep"

model:
  base_model: "deepseek-ai/DeepSeek-R1-Distill-Qwen-7B"

baseline:
  method: "independent"

task:
  name: "aime"
  n_problems: -1
