# Cross-arch v13 extension (llama × aime).
defaults:
  - ppfg

experiment:
  name: "llama31_aime_ppfg_stag"

model:
  base_model: "meta-llama/Llama-3.1-8B-Instruct"

ppfg:
  injection_rule: "stagnation"
  extract_threshold: 0.6
  k_min: 2
  k_max: 8
  max_injections_per_chain: 2

task:
  name: "aime"
  n_problems: -1
