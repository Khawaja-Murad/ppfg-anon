# Cross-arch v13 extension (llama × aime).
defaults:
  - default

experiment:
  name: "llama31_aime_indep"

model:
  base_model: "meta-llama/Llama-3.1-8B-Instruct"

baseline:
  method: "independent"

task:
  name: "aime"
  n_problems: -1
